<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Fill color (white inside) -->
    <solid android:color="@color/white" />

    <!-- Border -->
    <stroke
        android:width="1dp"
        android:color="@color/blue" />

    <!-- Size (optional, can be set in View instead) -->
    <size
        android:width="60dp"
        android:height="60dp" />

</shape>